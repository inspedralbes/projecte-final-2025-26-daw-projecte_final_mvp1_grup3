# ===========================================
# Loopy - Variables de entorno unificadas
# Copiar a .env y ajustar valores
# ===========================================

# --- Base de datos (PostgreSQL 16) ---
POSTGRES_USER=loopy
POSTGRES_PASSWORD=loopy_secret
POSTGRES_DB=loopy_db

# --- Redis (comunicación entre Node y Laravel) ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# --- Shared Secret JWT (mismo valor en Laravel y Node) ---
JWT_SECRET=change-me-to-a-long-random-string-in-production

# --- URLs internas Docker (opcional, para servicios que llamen entre sí) ---
# Frontend (Nuxt) usa estas en runtimeConfig para conectar al backend
SOCKET_URL=http://localhost:3001
API_URL=http://localhost:8000

# --- Laravel (backend-laravel) ---
# APP_KEY se genera con: php artisan key:generate
APP_KEY=
