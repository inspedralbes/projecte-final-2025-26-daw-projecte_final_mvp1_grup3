-- insert.sql: Datos iniciales o seeds (sintaxis PostgreSQL)
-- Ejecutar manualmente tras las migraciones si se desea: psql -U loopy -d loopy_db -f insert.sql

-- Ejemplo (descomentar y adaptar cuando existan tablas):
-- INSERT INTO users (name, email, ...) VALUES (...);
